# Drawing from: https://github.com/ogrisel/python-appveyor-demo/blob/master/appveyor.yml

version: 0.0.{build}-alpha

# init:
  # # Display remote desktop details
  # - ps: iex ((new-object net.webclient).DownloadString('https://raw.githubusercontent.com/appveyor/ci/master/scripts/enable-rdp.ps1'))

environment:
  # global:
    # MSYSTEM: "MINGW64"
    
    # SDK v7.0 MSVC Express 2008's SetEnv.cmd script will fail if the
    # /E:ON and /V:ON options are not enabled in the batch script intepreter
    # See: http://stackoverflow.com/a/13751649/163740
    # CMD_IN_ENV: "cmd /E:ON /V:ON /C .\\.appveyor\\run_with_env.cmd"

  matrix:
    # - PYTHON: "C:\\Miniconda35-x64"
      # PYTHON_VERSION: "3.5.x"
      # PYTHON_ARCH: "64"

      # - PYTHON: "C:\\Python35-x64"
      # PYTHON_VERSION: "3.5.x"
      # PYTHON_ARCH: "64"

    - PYTHON: "C:\\Miniconda-x64"
      PYTHON_VERSION: "2.7"
      PYTHON_ARCH: "64"

    # - PYTHON: "C:\\Python27-x64"
      # PYTHON_VERSION: "2.7.x"
      # PYTHON_ARCH: "64"
      
install:
    - "%APPVEYOR_BUILD_FOLDER%\\.appveyor\\setpath.bat"
    - "set PATH=%PYTHON%;%PYTHON%\\Scripts;%PATH%"
    - "del C:\\msys64\\Mingw64\\bin\\python.exe"
    - "conda create -q -y -n pyfvs_python --file %APPVEYOR_BUILD_FOLDER%\\requirements.txt"
    - "activate pyfvs_python"
    
build_script:
    - "%APPVEYOR_BUILD_FOLDER%\\.appveyor\\configure.bat"
    - "%APPVEYOR_BUILD_FOLDER%\\.appveyor\\build.bat"
    - "set ARCHIVE_NAME=open-fvs-%APPVEYOR_REPO_TAG_NAME%-Python%PYTHON_VERSION%_%PYTHON_ARCH%-windows-ci.zip"

after_build:
    - pushd bin\build
    - 7z a -bd %ARCHIVE_NAME% Open-FVS\*

on_failure:
    - appveyor PushArtifact bin\build\build_err.log
    - appveyor PushArtifact bin\build\f2py_*.log
    
artifacts:
    - path: bin\build\%ARCHIVE_NAME%
      name: open-fvs
      type: zip

deploy:
  # release: myproduct-v$(appveyor_build_version)
  description: 'Automated build release.'
  provider: GitHub
  auth_token:
    secure: zBeKVZXAsxJBErcR7yENdMW9tIXYu/q5tDVj/S6dMbWA1FAty6BDjCWRXZj6R0rG
  artifact: bin\build\%ARCHIVE_NAME%
  draft: true
  prerelease: false
  on:
    # branch: master                 # release from master branch only
    appveyor_repo_tag: true        # deploy on tag push only
    
# on_finish:
  # # Pause the build for remote desktop access
  # - ps: $blockRdp = $true; iex ((new-object net.webclient).DownloadString('https://raw.githubusercontent.com/appveyor/ci/master/scripts/enable-rdp.ps1'))